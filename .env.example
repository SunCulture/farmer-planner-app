# Copied to `.env` for local Expo / Metro. Never commit `.env`.
# EAS builds use the same name on the development / preview / production environments.
EXPO_PUBLIC_API_URL=https://tujiweze.fly.dev

# Optional local-only bearer token for DevAuthGate / authenticated API calls in __DEV__.
# Register or log in via Postman, then paste the accessToken here. Do not set in production.
# EXPO_PUBLIC_DEV_ACCESS_TOKEN=
